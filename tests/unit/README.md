# Unit Tests

Package-level unit tests should live beside the package code they verify.

Repository-level unit test helpers or cross-package unit suites may be placed here when a task explicitly requires them.