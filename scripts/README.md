# Scripts

Root-level project scripts live here when they apply to the whole repository.

Use Corepack for pnpm:

```powershell
corepack enable
corepack prepare pnpm@9.0.0 --activate
pnpm install
```
