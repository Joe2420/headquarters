# Shared Package

`@headquarters/shared` currently owns shared domain types and event contracts for the bootstrap phase.

This preserves the approved HQ-TASK-0000-FIX decision to keep `shared` as the current domain/shared package for now. A future architecture-approved task may split or rename this package if required.